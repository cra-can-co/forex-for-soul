// End-to-end flow lives at /scripts/init-devnet.ts (seed) and /scripts/push-prices.ts
// (oracle crank). Real verification is the app itself against devnet. No unit
// tests here by choice — Tier 3 scope.
export {};
